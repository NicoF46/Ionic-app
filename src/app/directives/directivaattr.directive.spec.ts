import { DirectivaattrDirective } from './directivaattr.directive';

describe('DirectivaattrDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaattrDirective();
    expect(directive).toBeTruthy();
  });
});
